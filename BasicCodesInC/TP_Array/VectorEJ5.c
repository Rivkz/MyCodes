#include<stdio.h>
#include<conio.h>

int main(){
	int n[]={2, 3, 4, 6, 8};
	int x, m=8, nm;
	
	for(x=0;x<5;x++){
		
		if(n[x] >= m){
			nm = n[x];
		}else{}
}
	printf("el mayor de los numeros es: %i", nm);
	getch();
	return 0;
}